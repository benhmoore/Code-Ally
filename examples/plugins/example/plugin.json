{
  "name": "reverse-string",
  "version": "1.0.0",
  "description": "Reverse a string using Python",
  "command": "python3",
  "args": ["reverse.py"],
  "requiresConfirmation": false,
  "author": "Ally Examples",
  "schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Text to reverse"
      },
      "preserve_case": {
        "type": "boolean",
        "description": "Whether to preserve the case of characters (default: true)"
      }
    },
    "required": ["text"]
  }
}
