/**
 * useActivityStream - Hook to access ActivityStream from context
 *
 * This is a convenience re-export of the context hook with a cleaner name.
 */

export { useActivityStreamContext as useActivityStream } from '../contexts/ActivityContext.js';
